

export const User = () => {
    const user = {
        name: 'clint',
        email: 'adsflahsdfkl'
    }
  return (
    <div>
        <p>Name: {user.name}</p>
        <p>Email: {user.email}</p>
    </div>
  )
}
